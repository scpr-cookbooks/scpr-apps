#!/bin/sh
exec 2>&1
exec chpst -u <%= @options[:user] %> /usr/bin/env <%= @options[:dir] %>/current/streammachine-runner-cmd <% if @options[:watch] %>--watch "<%= @options[:watch] %>" --restart <% end %>--title "<%= @options[:name] %>" --config <%= @options[:config] %> --dir <%= @options[:dir] %>/current