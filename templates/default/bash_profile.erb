export PATH=~/bin:<%= @bash_path ? "#{@bash_path}:" : "" %>$PATH

<% if @resource.app_type == :rails %>
export RAILS_ENV=<%= @key %>
<% end %>

<% (@resource.env||{}).each do |k,v| %>
export <%= k %>=<%= v %>
<% end %>

export PS1="[(\u@\h)-([\`date +\"%a %B %d %l:%M%p\"\`])]\n\w: "